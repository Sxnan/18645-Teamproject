#include "grid.h"
//constexpr auto MAX_COST = 100000;

/*
Grid::Grid(void)
{
	closed = false;
	cost = MAX_COST;
	prev_cost = MAX_COST;
	estimated_cost = MAX_COST;
}

Grid::Grid(int x, int y)
{
	cost = MAX_COST;
	prev_cost = MAX_COST;
	estimated_cost = MAX_COST;
}

bool Grid::operator<(const Grid & a) const
{
	// Smaller element has higher priority
	return cost > a.cost;
}


void Grid::SetPriority(int p)
{
	cost = p;
	return;
}

int Grid::GetCost(void) const
{
	return cost;
}
*/